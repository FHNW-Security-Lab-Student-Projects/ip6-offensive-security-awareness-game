# Godot 4+ specific ignores
.godot/
/android/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files; we
# parse the CSV at runtime via the I18n autoload, so the compiled
# .translation files are unused)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local planning notes (not for the repo)
.plans/

